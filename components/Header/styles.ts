import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  sectionTitle: {
    width: "100%",
    textAlign: "center",
    alignItems: "center",
    fontSize: 24,
    fontWeight: "bold",
  },
});

export default styles;
